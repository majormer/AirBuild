#include "AirBuildGameInstanceModule.h"

UAirBuildGameInstanceModule::UAirBuildGameInstanceModule()
{
	// Root module flag + the ModConfigurations entry (AirBuild_Config) are set on the Blueprint child.
}
